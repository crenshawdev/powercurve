name: Build and deploy to pacman repo

on:
  push:
    tags:
      - 'v*'

jobs:
  build-deploy:
    runs-on: arch-local
    steps:
      - uses: https://code.forgejo.org/actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build package
        run: |
          export CARGO_TARGET_DIR=target
          makepkg -sf --noconfirm

      - name: Deploy to arch repo
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
        run: |
          tag="${GITHUB_REF_NAME}"
          pkg="$(pwd)/$(ls powercurve-git-*-x86_64.pkg.tar.zst | head -1)"

          rm -rf /tmp/repo
          git clone --branch pages \
            "https://VintageTechie:${DEPLOY_TOKEN}@codeberg.org/VintageTechie/vintagetechie-arch-repo.git" \
            /tmp/repo

          # clean up old package name from the rename
          rm -f /tmp/repo/x86_64/vintagetechie-power-git-*.pkg.tar.zst
          cd /tmp/repo/x86_64
          repo-remove vintagetechie-arch-repo.db.tar.gz vintagetechie-power-git 2>/dev/null || true

          rm -f powercurve-git-*.pkg.tar.zst
          cp "$pkg" .
          repo-add vintagetechie-arch-repo.db.tar.gz "$(basename "$pkg")"

          cd /tmp/repo
          git config user.name "VintageTechie"
          git config user.email "noreply@codeberg.org"
          git add -A
          git commit -m "update powercurve-git to ${tag}"
          git push origin pages
